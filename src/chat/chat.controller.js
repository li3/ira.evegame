(function(ng) {

  var manifest = {
    name: "ChatController",
    type: "controller",
    module: 'app',
    dependencies: []
  };

  var ChatController = function() {
    var self = this;
  };

  ng.registerManifest(manifest, ChatController);
}(angular));