(function(ng) {

  var manifest = {
    name: "HomeController",
    type: "controller",
    module: 'app',
    dependencies: []
  };

  var HomeController = function() {
    var self = this;
  };

  ng.registerManifest(manifest, HomeController);
}(angular));