(function(ng) {

  var manifest = {
    name: "HomeController",
    type: "controller",
    module: 'app',
    dependencies: []
  };

  var implementation = function() {
    var self = this;
  };

  ng.registerManifest(manifest, implementation);
}(angular))