(function(ng) {

  var manifest = {
    name: "ChatController",
    type: "controller",
    module: 'app',
    dependencies: []
  };

  var implementation = function() {
    var self = this;
  };

  ng.registerManifest(manifest, implementation);
}(angular))